/dist/
*.log
*.tmp
.DS_Store
dist/

# packaged apps
*-web/
*-web-opt/
*-android/
*-ios/

# donor-fetch credentials (never commit)
donors.env
/etc/qorm/

# internal planning / aspirational specs — local only, not part of the public runtime docs
/planning/

# stray build artifacts (never commit — see the qorm-wasm/render.test slip)
/qorm
/qorm-wasm
/qorm-desktop-*
*.test
*.wasm
# optimized wasm build outputs (targeted — do not broaden to block demo assets)
/qorm_optimized.wasm
/qorm_optimized.wasm.gz
BUNDLE_ONLY

# private operations material (go-to-market, drafts) — local only
/ops/internal/
/site/
/web_server/