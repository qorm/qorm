{
  "type": "action",
  "id": "drag_move_1",
  "steps": [
    { "type": "state.set", "path": "n1x", "value": "{{ state.drag1_ox + (x - state.drag1_sx) }}" },
    { "type": "state.set", "path": "n1y", "value": "{{ state.drag1_oy + (y - state.drag1_sy) }}" }
  ]
}
